main.py : 執行main.py即可進行前半部的資料前處理工作，即1.Data preprocess(first)

Output : 對應每個小階段的output結果會存放在Output資料夾中

preprocess_function.py : 存放此階段工作所需要使用到的function

Statistical analysis(execute after Data preprocess(first)) : 先將1.Data preprocess(first)中的main.py執行完，得到Output後再來此資料夾進行統計分析